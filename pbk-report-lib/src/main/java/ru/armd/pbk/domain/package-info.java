/**
 * Пакет с классами-структурами описывающими доменную модель. Эти классы используюутся в качествет сущностей для ORM.
 * На этих классах реализуется бизнес-логика приложения.
 * НЕДОПУСТИМО передавать объекты этих классов НА клиента.
 */
package ru.armd.pbk.domain;