/**
 * Пакет с абстракцией классов-структур описывающими доменную модель.
 * Домены используюутся в качествет сущностей для ORM.
 * На этих классах реализуется бизнес-логика приложения.
 * НЕДОПУСТИМО передавать объекты этих классов НА клиента.
 */
package ru.armd.pbk.core.domain;